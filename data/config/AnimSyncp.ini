; AnimSync - Basic template config file

; Loads regular config data
@AnimSync.ini@

; Overrides some values for profile
[Display]
Title           = AnimSync (Profile)
ShowFPS         = true
